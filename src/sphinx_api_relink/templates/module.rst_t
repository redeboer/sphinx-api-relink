{%- if show_headings and not separate %}
# {{ basename.split(".")[-1] }}

```python
import {{ basename }}
```

{% endif -%}
```{eval-rst}
.. automodule:: {{ qualname }}
{%- for option in automodule_options %}
  :{{ option }}:
{%- endfor %}
```
